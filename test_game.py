# Will write a Pytest for: class Room(object):

  

@pytest.fixture()
def test_add_paths():
    center = Room("Center", 'i mitten')

    center.add_paths(['north'] = north)


def test_stigen(test_add_paths):
    paths == 'north'
